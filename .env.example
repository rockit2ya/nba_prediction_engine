# The Odds API — https://the-odds-api.com
# Sign up for a free API key (500 requests/month) to enable CLV tracking.
# Without this key, the engine works fine — CLV features are simply skipped.
ODDS_API_KEY=your_api_key_here
